# Day37（2026.08.03）

## 今日目标

- 理解 VGG 网络的设计思想，掌握"用小卷积核堆叠替代大卷积核"来构建深层网络的方法。
- 掌握 VGG Block 的设计思想与 VGG16 / VGG19 的网络结构差异。
- 在 `pytorch/Day37/` 目录下完成 `VGG.py` 代码实现（VGG Block、VGG 网络搭建、参数量统计），并完成 `CNN经典网络结构.md` 的整理，提交代码至 GitHub。

## 完成情况

- - [ ] 观看视频
- - [ ] 环境配置 / 代码实验
- - [ ] 完成实验程序
- - [ ] 提交代码至 GitHub

## 今日任务拆解（建议总耗时 5.0~5.5 小时）

### 第一阶段：观看视频（预估 2.5~3.0 小时）

在 PyCharm 中跟随学习，重点理解 VGG 用小卷积核堆叠构建深层网络的思路：

- **李沐《动手学深度学习》**：
  - **第82讲 VGG**：理解 VGG 的核心思想（用多个 3×3 卷积替代大卷积核），掌握 VGG Block 的设计与 VGG16 / VGG19 的结构差异。
  - **第83讲 VGG代码实现**：跟随代码实现 `vgg_block` 与 `vgg(conv_arch)`，理解"用卷积配置列表灵活生成不同深度 VGG"的写法。

预计时长：3 小时

### 第二阶段：理论学习（约 1.0 小时）

系统梳理 VGG 的设计思想、Block 结构与三代经典网络的演进脉络：

- **核心知识点**：

  - **VGG 核心思想**：

    ```
    多个3×3卷积
           ↓
    替代大卷积核
           ↓
    增加网络深度
    ```

  - **为什么用小卷积核堆叠？**（相同感受野下参数量更少、非线性更强）

    | 方案 | 感受野 | 参数量（各层通道数均为 $C$） |
    |---|---|---|
    | 1 个 5×5 卷积 | 5×5 | $25C^2$ |
    | **2 个 3×3 卷积** | 5×5 | $2 \times 9C^2 = \mathbf{18C^2}$ |
    | 1 个 7×7 卷积 | 7×7 | $49C^2$ |
    | **3 个 3×3 卷积** | 7×7 | $3 \times 9C^2 = \mathbf{27C^2}$ |

    → 堆叠 3×3 卷积既**减少了参数量**，又因为每层卷积之间都插入 ReLU 而**增强了非线性表达能力**。

  - **VGG Block 设计思想**：把「若干个 3×3 卷积（`padding=1` 保持尺寸）+ 1 个 2×2 最大池化（尺寸减半）」打包成一个**可复用的块**，整个网络就是若干 Block 的顺序堆叠。

  - **VGG16 结构**：**13 个卷积层 + 3 个全连接层**，共 5 个 Block
  - **VGG19 结构**：**16 个卷积层 + 3 个全连接层**，比 VGG16 在 Block3/4/5 各多一个卷积层

### 第三阶段：代码实验（约 1.0 小时）

#### 目录结构演进

在 `GeoAI-Learning` 项目的 `pytorch` 目录下新建 `Day37` 文件夹及代码文件：

```
GeoAI-Learning/
│
└── pytorch/
    ├── Day36/
    └── Day37/
        └── VGG.py
```

#### 实验任务

创建 `VGG.py`，在 `pytorch/Day37/VGG.py` 中实现 VGG Block、按配置搭建 VGG 网络，并统计参数量：

```python
import torch
import torch.nn as nn


def vgg_block(num_convs, in_channels, out_channels):
    """VGG 块：num_convs 个 3x3 卷积（padding=1 保持尺寸）+ 1 个 2x2 最大池化"""
    layers = []
    for _ in range(num_convs):
        layers.append(nn.Conv2d(in_channels, out_channels, kernel_size=3, padding=1))
        layers.append(nn.ReLU(inplace=True))
        in_channels = out_channels          # 块内后续卷积的输入通道 = 本块的输出通道
    layers.append(nn.MaxPool2d(kernel_size=2, stride=2))   # 每个块结束后 H/W 减半
    return nn.Sequential(*layers)


def vgg(conv_arch, num_classes=1000):
    """按卷积配置列表 conv_arch 搭建 VGG 网络"""
    conv_blks = []
    in_channels = 3
    for (num_convs, out_channels) in conv_arch:
        conv_blks.append(vgg_block(num_convs, in_channels, out_channels))
        in_channels = out_channels
    return nn.Sequential(
        *conv_blks,
        nn.Flatten(),                                    # (N, 512, 7, 7) -> (N, 25088)
        nn.Linear(out_channels * 7 * 7, 4096),           # 512 * 7 * 7 = 25088
        nn.ReLU(inplace=True),
        nn.Dropout(p=0.5),
        nn.Linear(4096, 4096),
        nn.ReLU(inplace=True),
        nn.Dropout(p=0.5),
        nn.Linear(4096, num_classes),
    )


def main():
    # 卷积配置列表：(每个块的卷积层数, 输出通道数)
    conv_arch_vgg11 = ((1, 64), (1, 128), (2, 256), (2, 512), (2, 512))
    conv_arch_vgg16 = ((2, 64), (2, 128), (3, 256), (3, 512), (3, 512))

    for name, arch in (("VGG11", conv_arch_vgg11), ("VGG16", conv_arch_vgg16)):
        net = vgg(arch)
        x = torch.randn(1, 3, 224, 224)
        print("=" * 60)
        print(f"{name} 各阶段特征图尺寸：")
        with torch.no_grad():
            for i, blk in enumerate(net):
                x = blk(x)
                if i < 5:
                    print(f"  Block{i + 1} 输出: {tuple(x.shape)}")

        # 参数量统计
        total = sum(p.numel() for p in net.parameters())
        conv_params = sum(p.numel() for p in net[:5].parameters())
        fc_params = sum(p.numel() for p in net[5:].parameters())
        print(f"  总参数量: {total:,} ({total / 1e6:.1f} M)")
        print(f"    卷积部分: {conv_params:,} ({conv_params / total * 100:.1f}%)")
        print(f"    全连接部分: {fc_params:,} ({fc_params / total * 100:.1f}%)")
        print(f"  最终输出 Shape: {tuple(net(torch.randn(1, 3, 224, 224)).shape)}")


if __name__ == "__main__":
    main()
```

运行输出（关键信息）：

```
============================================================
VGG11 各阶段特征图尺寸：
  Block1 输出: (1, 64, 112, 112)
  Block2 输出: (1, 128, 56, 56)
  Block3 输出: (1, 256, 28, 28)
  Block4 输出: (1, 512, 14, 14)
  Block5 输出: (1, 512, 7, 7)
  总参数量: 132,863,336 (132.9 M)
    卷积部分: 9,220,480 (6.9%)
    全连接部分: 123,642,856 (93.1%)
  最终输出 Shape: (1, 1000)
============================================================
VGG16 各阶段特征图尺寸：
  Block1 输出: (1, 64, 112, 112)
  Block2 输出: (1, 128, 56, 56)
  Block3 输出: (1, 256, 28, 28)
  Block4 输出: (1, 512, 14, 14)
  Block5 输出: (1, 512, 7, 7)
  总参数量: 138,357,544 (138.4 M)
    卷积部分: 14,714,688 (10.6%)
    全连接部分: 123,642,856 (89.4%)
  最终输出 Shape: (1, 1000)
```

#### 维度推导验证（VGG16）

| 阶段 | 配置 | 输入 Shape | 输出 Shape | 计算依据 |
|---|---|---|---|---|
| 输入 | — | — | $(N, 3, 224, 224)$ | ImageNet 图像尺寸 |
| Block1 | 2 × `Conv(3→64, k=3, p=1)` + `MaxPool(2,2)` | $(N, 3, 224, 224)$ | $(N, 64, 112, 112)$ | $224 / 2 = 112$ |
| Block2 | 2 × `Conv(64→128, k=3, p=1)` + `MaxPool(2,2)` | $(N, 64, 112, 112)$ | $(N, 128, 56, 56)$ | $112 / 2 = 56$ |
| Block3 | 3 × `Conv(128→256, k=3, p=1)` + `MaxPool(2,2)` | $(N, 128, 56, 56)$ | $(N, 256, 28, 28)$ | $56 / 2 = 28$ |
| Block4 | 3 × `Conv(256→512, k=3, p=1)` + `MaxPool(2,2)` | $(N, 256, 28, 28)$ | $(N, 512, 14, 14)$ | $28 / 2 = 14$ |
| Block5 | 3 × `Conv(512→512, k=3, p=1)` + `MaxPool(2,2)` | $(N, 512, 14, 14)$ | $(N, 512, 7, 7)$ | $14 / 2 = 7$ |
| **展平** | `Flatten` | $(N, 512, 7, 7)$ | $(N, 25088)$ | $512 \times 7 \times 7$ |
| FC1 | `Linear(25088→4096)` | $(N, 25088)$ | $(N, 4096)$ | — |
| FC2 | `Linear(4096→4096)` | $(N, 4096)$ | $(N, 4096)$ | — |
| FC3 | `Linear(4096→1000)` | $(N, 4096)$ | $(N, 1000)$ | 1000 个类别 |

> **一句话规律**：Block 内所有 3×3 卷积因 `padding=1` **尺寸不变**；每个 Block 末尾的 `MaxPool(2,2)` 让 H/W **减半**。
> 所以 VGG16 的尺寸链是：$224 \to 112 \to 56 \to 28 \to 14 \to 7$，展平维度 $512 \times 7 \times 7 = 25088$。

#### 参数量分析（重要观察）

| 部分 | 参数量 | 占比 |
|---|---|---|
| 5 个卷积 Block（13 个卷积层） | 14,714,688 | 10.6% |
| 3 个全连接层 | 123,642,856 | **89.4%** |
| **↳ 其中 FC1 一层** | **102,764,544** | **74.3%** |
| **合计** | **138,357,544** | 100% |

**结论**：VGG16 虽然卷积层多达 13 个，但**参数量 89.4% 都在全连接层**，而**第一个全连接层 `Linear(25088→4096)` 一层就占了全网的 74.3%**。

> 这与 AlexNet（全连接层占 96%）是同一个问题：**展平维度越大，第一个全连接层越臃肿**。这也是后续网络（NiN / GoogLeNet / ResNet）用**全局平均池化**替代巨型全连接层的直接动机。
>
> 另一个好处是：**卷积层的参数量与输入图像尺寸无关**（权重共享），而全连接层会随展平维度线性增长。

### 第四阶段：输出成果（约 0.5 小时）

在 `notes/` 目录下完成 `CNN经典网络结构.md`，梳理三代经典网络的演进：

- 记录 **LeNet、AlexNet、VGG** 三者的结构对比
- 主线：**网络越来越深、卷积核越来越小、参数量分布越来越集中在全连接层**

| 网络 | 年份 | 卷积层数 | 全连接层数 | 关键创新 |
|---|---|---|---|---|
| **LeNet** | 1998 | 2 | 3 | 最早成功的 CNN，用于手写数字识别 |
| **AlexNet** | 2012 | 5 | 3 | ReLU、Dropout、GPU 训练、数据增广 |
| **VGG** | 2014 | 13 / 16 | 3 | **3×3 小卷积核堆叠**、Block 模块化设计 |

### 第五阶段：GitHub 提交（约 15 分钟）

在终端验证代码运行无误后提交：

```bash
# 1. 检查变动
git status

# 2. 将变动加入暂存区
git add .

# 3. 提交规范 Commit 信息
git commit -m "Day37：完成VGG网络学习"

# 4. 推送至 GitHub
git push
```

## 今日需要输出的成果

### 本地与 GitHub 仓库

- 创建 `pytorch/Day37/` 目录。
- 完成 `VGG.py`：VGG Block 实现、VGG 网络搭建与参数量统计。
- 在 `notes/` 目录下生成 `CNN经典网络结构.md`。

### 笔记沉淀

- 完成 Day37 详细笔记，记录 VGG 核心思想、Block 设计思想、VGG16/VGG19 结构与各层维度变化推导。

### 截图建议（可选，建议保存在个人笔记中）

**VGG 网络结构与参数量统计运行截图**：截取 PyCharm 控制台中运行 `VGG.py` 输出的各阶段特征图尺寸、参数量统计（卷积部分 vs 全连接部分）及 forward 输出张量维度。



**GitHub 阶段更新截图**：截取浏览器或 Git 客户端中成功提交 `pytorch/Day37/` 目录及 Commit 记录 `"Day37：完成VGG网络学习"` 的界面。



## Day37 完成标准

睡前满足以下 4 项即为 100% 完成：

1. 理解 VGG 的核心设计思想，能说清"为什么用多个 3×3 卷积替代大卷积核"（参数量更少、非线性更强）。
2. 掌握 VGG16 与 VGG19 的结构差异，能够推导数据经过每个 Block 后的 Shape 变化。
3. 独立完成 `VGG.py` 中 VGG Block 实现、网络搭建与参数量统计。
4. 成功推送 `pytorch/Day37/VGG.py` 与 `CNN经典网络结构.md` 至 GitHub 仓库。

## 今日完成

- 完成了李沐《动手学深度学习》第82讲（VGG）与第83讲（VGG代码实现）的视频学习。
- 理解了 VGG 的核心思想：用多个 3×3 小卷积核堆叠替代大卷积核，在相同感受野下参数量更少、非线性更强。
- 掌握了 VGG Block 的模块化设计（若干 3×3 卷积 + 1 个 2×2 最大池化），以及 VGG16（13 卷积 + 3 全连接）与 VGG19（16 卷积 + 3 全连接）的结构差异。
- 在 `pytorch/Day37/` 目录下完成了 `VGG.py` 的编写，实现了 `vgg_block` 与 `vgg(conv_arch)`，并完成了 VGG11 / VGG16 的搭建、维度验证与参数量统计。
- 完成了 `CNN经典网络结构.md` 的整理，梳理了 LeNet → AlexNet → VGG 的演进脉络。
- 成功将 Day37 实验代码及笔记提交并推送至 GitHub 仓库。

## 今日收获

**第82讲 VGG**：



**第83讲 VGG代码实现**：



## 遇到的问题/注意事项



## 明日计划

- 开启 ResNet 残差网络的学习，理解网络加深后出现的梯度消失问题及残差连接的解决思路。
- 观看李沐《动手学深度学习》第94讲（ResNet）、第95讲（ResNet代码实现）与第97讲（ResNet梯度计算）。
- 在 `pytorch/Day38/` 目录下完成 `ResNet_Block.py` 代码实现（Residual Block、简化 ResNet），并提交至 GitHub。

## 附件：CNN经典网络结构

# CNN经典网络结构.md

