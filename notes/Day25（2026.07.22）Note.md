# Day25（2026.07.22）

## 今日目标

- 掌握 PyTorch 神经网络的基本搭建流程与规范（基于 `nn.Module`）。
- 熟练编写前向传播 `forward` 函数，理解模型参数的定义与自动管理机制。
- 理解 `nn.Linear`、激活函数以及 Softmax 输出在神经网络中的作用。
- 在 `pytorch/Day25/` 目录下创建 `mlp_classifier.py` 脚本，完成多层感知机（MLP）网络搭建、前向传播实验以及模型参数分析，并推送至 GitHub。

## 完成情况

- - [x] 观看视频  
- - [x] 环境配置 / 代码实验  
- - [x] 完成实验程序  
- - [x] 提交代码至 GitHub  

## 今日任务拆解（建议总耗时 3.0~3.5 小时）

### 第一阶段：观看视频（预估 1.5 小时）

在 PyCharm 中跟随敲写神经网络搭建与分类训练代码，重点理解网络模块构造与训练循环闭环：  

- **Day04-03 神经网络_搭建流程介绍**（约 20 分钟）：理解继承 `nn.Module` 类的基本规范、`__init__` 中层结构的定义以及 `forward` 函数在前向传播中的调度机制。  
- **Day04-04 神经网络_搭建代码实现**（约 25 分钟）：掌握使用 `nn.Linear` 与激活函数组合构建多层感知机（MLP）的代码实现细节。  
- **Day04-05 神经网络_模型训练**（约 25 分钟）：掌握完整训练循环的核心步骤，包括批次数据迭代、梯度清空、前向计算、损失求解、反向传播及优化器更新。  
- **Day04-06 神经网络_总结**（约 20 分钟）：梳理神经网络搭建、训练与评估的标准化流程。  

### 第二阶段：理论学习（约 0.5 小时）

深入理解 PyTorch 神经网络架构与训练机制：  

- **`nn.Module` 核心结构**： 所有的自定义神经网络必须继承 `torch.nn.Module`，并在 `__init__` 中调用 `super().__init__()`。定义在 `__init__` 中的层（如 `nn.Linear`）其参数（`weight` 与 `bias`）会被自动注册到模型参数列表中。  
- **前向传播机制**：直接调用 `model(x)` 会触发 `nn.Module` 的 `__call__` 方法，内部自动执行 `forward(x)` 完成数据从输入层到输出层的计算。

### 第三阶段：代码实验（约 1 小时）

#### 目录结构演进

在 `GeoAI-Learning` 项目的 `pytorch` 目录下新建 `Day25` 文件夹：  

```
GeoAI-Learning/
│
├── python_basic/
│   ├── Day14/
│   └── Day15/
│
└── pytorch/
    ├── Day16/
    ├── Day17/
    ├── ...
    ├── Day24/
    └── Day25/
        └── mlp_classifier.py
```

#### 实验任务

创建 `mlp_classifier.py` 文件，编写并运行以下代码，完成多层感知机（MLP）网络搭建、模拟输入生成、前向传播测试以及模型参数分析：

```python
"""
程序名称：mlp_classifier.py
作者：黄乐源
日期：2026-07-22
描述：PyTorch 多层感知机 (MLP) 网络结构搭建、前向传播与模型参数分析实战
"""

# 导包
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import TensorDataset, DataLoader
import matplotlib.pyplot as plt
from torchsummary import summary

"""
print("================= 任务 1：构建分类数据集 =================")

# 随机生成 3 簇 2 维分类数据（模拟 3 分类任务）
torch.manual_seed(42)       # 设置随机种子

n_samples = 300
class_0 = torch.randn(n_samples, 2) + torch.tensor([-2.0, -2.0])   # 先生成300行2列的正态分布数据，再将所有数据向左下偏移2个单位，即生成中心在(-2, -2)的一团点
class_1 = torch.randn(n_samples, 2) + torch.tensor([2.0, 2.0])     # 先生成300行2列的正态分布数据，再将所有数据向右上偏移2个单位，即生成中心在(2, 2)的一团点
class_2 = torch.randn(n_samples, 2) + torch.tensor([-2.0, 2.0])    # 先生成300行2列的正态分布数据，再将所有数据向左上偏移2个单位，即生成中心在(-2, 2)的一团点

x = torch.cat([class_0, class_1, class_2], dim = 0)     # 按行合并三个类别的数据
y = torch.cat([torch.zeros(n_samples), torch.ones(n_samples), torch.full((n_samples,), 2)], dim = 0).long()   # 生成类别编号，类型为int64

# 划分训练集与测试集 (8:2) -> 把前面生成的900个样本随机打乱，然后按照8:2比例划分为训练集和测试集。
# 训练集（train set）：用于模型学习参数
indices = torch.randperm(x.size(0))     # 生成一个随机排列的整数序列当作索引，数量与x的第0维大小相同。
x, y = x[indices], y[indices]           # 按随机索引重新排列数据

train_size = int(0.8 * len(x))      # 定义训练集大小，这里为：0.8 * 900 = 720；从而得到测试集大小为900 - 720 = 180
x_train, x_test = x[:train_size], x[train_size:]        # 取x的前720个为训练集，后180个为测试集
y_train, y_test = y[:train_size], y[train_size:]        # 同步划分标签，取y的前720个为训练标签，后180个为测试标签

# 构造 Dataset -> DataLoader
train_dataset = TensorDataset(x_train, y_train)
# 参1：数据集对象；参2：批次大小（每批样本数量）；参3：是否打乱数据（如果不打乱，多轮训练时每轮拿到的值是一样的）（训练集打乱，测试集不打乱）
train_loader = DataLoader(train_dataset, batch_size = 32, shuffle = True)

print(f"训练集样本数：{len(x_train)}，测试集样本数：{len(x_test)}")
"""

print("\n=================任务1：创建模拟输入数据=================")

data = torch.randn(size=(5,3))

print("输入数据：", data)
print("输入数据shape:", data.shape)


print("\n================= 任务 2：搭建 MLP 分类网络 =================")

class MLPClassifier(nn.Module):
    def __init__(self, input_dim, hidden_dim, num_classes):
        super(MLPClassifier, self).__init__()
        self.fc1 = nn.Linear(input_dim, hidden_dim)
        self.fc2 = nn.Linear(hidden_dim, hidden_dim)
        self.output = nn.Linear(hidden_dim, num_classes)

        # 参数初始化
        nn.init.kaiming_normal_(self.fc1.weight, nonlinearity='relu')
        nn.init.kaiming_normal_(self.fc2.weight, nonlinearity='relu')

    def forward(self, x):
        # 第一层 隐藏层：加权求和 + 激活函数ReLU
        x = torch.relu(self.fc1(x))
        # 第二层 隐藏层：加权求和 + 激活函数ReLU
        x = torch.relu(self.fc2(x))
        # 第三层 输出层：加权求和 + 激活函数Softmax（因为本案例做的是三分类）
        output = torch.softmax(self.output(x), dim = -1)  # dim = -1在此处表示按行计算，一条样本一条样本地处理
        return output


print("\n================= 任务 3：模型前向传播实验 =================")

# 1. 创建模型
my_model = MLPClassifier(input_dim = 3, hidden_dim = 16, num_classes = 3)
print("网络结构：", my_model)

# 2. 创建模拟输入数据
# 5个样本，每个样本3个特征
data = torch.randn(size=(5, 3))
print("\n输入数据:", data)
print("\n输入数据形状:", data.shape)

# 3. 模型进入预测模式
my_model.eval()

# 4. 前向传播
with torch.no_grad():
    output = my_model(data)

print("\n模型输出:", output)
print("\n输出形状:", output.shape)

# 5. 查看每个样本预测概率
for i in range(len(output)):
    print(f"\n第{i + 1}个样本:")
    print("类别概率:", output[i])

    # 最大概率对应类别
    pred_class = torch.argmax(output[i])
    print("预测类别:", pred_class.item())
    print("-" * 30)

print("\n================= 任务 4：模型参数分析 =================")

print("==================计算模型参数==================")
# 参1：（神经网络）模型对象；参2：输入数据的维度(5, 3)
summary(my_model, input_size=(5, 3))        # 此处input_size的shape与输入数据集data相同。在输出结果的Output Shape中，-1代表自动计算，表示不确定

print("==================查看模型参数==================")
for name, param in my_model.named_parameters():
    print("name：", name)
    print("param：", param)
    print()

print("\n🏁 Day25 MLP 分类网络训练实战成功完成！")
```

### 第四阶段：GitHub 提交（约 15 分钟）

在终端验证代码无误后提交：  

```bash
# 1. 检查变动
git status

# 2. 将变动加入暂存区
git add .

# 3. 提交规范 Commit 信息
git commit -m "Day25：完成PyTorch MLP网络结构搭建"

# 4. 推送至 GitHub
git push
```

## 今日需要输出的成果

### 本地与 GitHub 仓库

- 建立 `pytorch/Day25/` 目录结构。  
- 完成 `pytorch/Day25/mlp_classifier.py` 代码文件。  

### 笔记沉淀

- 整理继承 `nn.Module` 构建神经网络的标准规范，总结 `__init__` 与 `forward` 方法的作用分工。  
- 理解神经网络前向传播过程，掌握输入数据经过线性层与激活函数逐层计算得到输出结果的流程。  
- 了解后续模型训练阶段涉及的损失函数、反向传播和参数更新流程，为后续学习 Loss 与 Optimizer 做准备。

### 截图建议（可选，建议保存在个人笔记中）

**控制台运行结果截图**：运行 `mlp_classifier.py`，截取显示模型前向传播输出结果以及模型结构信息。

![Day25_MlpClassifier运行结果](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day25\Day25_MlpClassifier运行结果.png)

**GitHub 阶段更新截图**：截取浏览器或 Git 客户端中，成功出现 `pytorch/Day25/` 目录以及 Commit 提交记录 `"Day25：完成MLP分类网络训练"` 的界面。  

![Day25_Github仓库](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day25\Day25_Github仓库.png)

## Day25 完成标准

睡前满足以下 4 项即为 100% 完成：  

1. 能够独立使用 `nn.Module` 继承方式书写一个简单的多层感知机（MLP）网络结构。  
2. 能不依赖模板完整默写出神经网络训练的标准五步代码（`zero_grad -> forward -> loss -> backward -> step`）。  
3. 理解分类任务中使用 `torch.argmax` 求解预测类别并计算测试集 Accuracy 的逻辑。  
4. 成功在 GitHub 仓库中创建 `pytorch/Day25/` 并推送 `mlp_classifier.py` 代码。  

## 今日完成

- 完成黑马 AI 大模型《神经网络与深度学习》（Day04-03 至 Day04-06）视频学习。
- 掌握 PyTorch 中基于 `nn.Module` 搭建神经网络的标准流程。
- 理解 `__init__()` 方法负责网络结构定义，`forward()` 方法负责数据前向传播。
- 独立编写并运行 `mlp_classifier.py`，完成 MLP 网络结构搭建、模型输出测试以及参数信息查看。
- 使用 `named_parameters()` 查看网络权重和偏置参数，并统计模型参数数量。
- 完成 GitHub 中 `pytorch/Day25/` 目录创建与代码提交。

## 今日收获

**Day04-03 至 Day04-04 神经网络搭建流程与代码实现**：  

- 在PyTorch中**定义深度神经网络**其实就是**层堆叠**的过程，继承自`nn.Module`，实现两个方法：
  - `__init__`方法中**定义网络中的层结构**，主要是**全连接层**，并进行**初始化**；（简单来说就是用于**定义层结构并初始化**）
  - `forward`方法，在实例化模型的时候，底层会**自动调用**该函数。该函数中**为初始化定义的layer传入数据**，进行**前向传播**等。
- **神经网络搭建流程**：
  - 1、定义一个类，继承：`nn.module`；
  - 2、在`__init__()`方法中，初始化父类对象，搭建神经网络；
  - 3、在`forward`方法中，完成：前向传播。
- **深度学习案例的4个步骤**：
  - 1、准备数据；
  - 2、搭建神经网络（数据预处理）；
  - 3、模型训练（反向传播就在这一步中进行）；
  - 4、模型测试。

**Day04-05 神经网络模型训练**：  

本节所有知识点已记录在对应py文件中。

**Day04-06 神经网络总结**：  

- 神经网络搭建和参数计算
  - 模型参数的计算：
    - 1、以第一个隐藏层为例：该隐层有3个神经元，每个神经元的参数为：4个（w1, w2, w3, b1），所以一共用3 x 4 = 12个参数。
    - 2、输入数据（x）和网络权重（w）是两个不同的事儿！对于初学者理解这一点十分重要，要分得清。

- 总结：

  - 神经网络的搭建方法：
    - 1、定义继承自`nn.module`的模型类；
    - 2、在`__init__()`方法中，定义网络中的层结构；
    - 3、在`forward`方法中，定义数据传输方式。

  - 网络参数量的统计方法：
    - 统计每一层中的权重w和偏置b的数量。
  - 神经网络的优缺点：
    - 优点：
      - 精度高，性能优于其他的机器学习方法，甚至在某些领域超过了人类；
      - 可以近似任意的非线性函数（可以做二分类、多分类等）；
      - 近年来在学界和业界受到了热捧，有大量的框架和库可供调。
    - 缺点：
      - 黑箱，很难解释模型是怎么工作的；
      - 训练时间长，需要大量的计算力；
      - 网络结构复杂，需要调整超参数；
      - 小数据集上表现不佳，容易发生过拟合。

## 遇到的问题/注意事项

注意：诸如`__init__`方法这样的魔法方法，不需要手动调用，在特定场合下会被自动调用。此外，`forward`方法虽然不是魔法方法，但**其用法与魔法方法一致**。

经验之谈：一般来说，每层的神经元个数是逐层递减的，换言之，第一个隐藏层的神经元数量最多，往后的层一般是更少的。

## 明日计划

- 攻克 Day26：理解不同任务对应的损失函数（`CrossEntropyLoss`, `MSELoss`, `MAELoss`, `SmoothL1Loss`），完成 `loss_function.py`！  
- 在 `pytorch/Day26/` 目录下完成实验代码并提交 GitHub。  