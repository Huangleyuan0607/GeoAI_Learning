# Day24（2026.07.21）

## 今日目标

- 掌握常用激活函数（ReLU、Softmax）的作用机制、数学公式及 PyTorch API 调用方式。  
- 理解为什么神经网络必须引入非线性激活函数，以及不同激活函数的适用场景与优缺点。  
- 掌握神经网络参数初始化（Xavier/Glorot 初始化、Kaiming/He 初始化）的基本原理与 PyTorch 代码实现。  
- 在 `pytorch/Day24/` 目录下创建 `activation_function.py` 脚本，完成 Sigmoid、Tanh、ReLU 函数可视化绘制、Softmax 概率计算与参数初始化对比实验，并推送至 GitHub。  

## 完成情况

- - [x] 观看视频  
- - [x] 环境配置 / 代码实验  
- - [x] 完成实验程序  
- - [x] 提交代码至 GitHub  

## 今日任务拆解（建议总耗时 3.0~3.5 小时）

### 第一阶段：观看视频（预估 1.5~2.0 小时）

在 PyCharm 中跟随敲写激活函数对比与参数初始化实验代码，重点理解非线性映射与网络权重初始化的作用：  

- **Day03-11 ReLU激活函数介绍**（约 20 分钟）：理解 ReLU 函数的数学定义、单侧抑制特性、死区问题（Dying ReLU）以及为什么它能极大缓解深度网络中的梯度消失问题。  
- **Day03-12 Softmax激活函数介绍**（约 25 分钟）：掌握 Softmax 函数的概率映射机制、数值稳定性技巧（防止指数溢出）以及在多分类任务中的应用。  
- **Day04-01 参数初始化_介绍**（约 20 分钟）：理解为什么不能将权重全初始化为 0（对称性破坏问题），掌握均匀分布/正态分布初始化、Xavier 初始化与 Kaiming 初始化的基本原理。  
- **Day04-02 参数初始化_代码演示**（约 25 分钟）：学习使用 `torch.nn.init` 模块对网络层的 `weight` 和 `bias` 进行自定义初始化的标准范式。  

### 第二阶段：理论学习（约 0.5 小时）

深入理解非线性激活函数与参数初始化的核心理论：  

- **ReLU 激活函数**：
  $$
  f(x) = \max(0, x)
  $$
  在 x > 0 时梯度恒为 1，彻底解决了正半轴的梯度消失问题，计算速度极快；在 x ≤ 0 时输出为 0，具有稀疏激活特性。  

- **Softmax 激活函数**：
  $$
  S_i = \frac{e^{z_i}}{\sum_{j=1}^{C} e^{z_j}}
  $$
  将包含 C 个类别得分的 logits 向量映射为总和为 $1$ 的概率分布（每个概率区间均在 (0, 1)）。  

- **参数初始化策略**：

  - **Xavier (Glorot) 初始化**：适用于 Sigmoid / Tanh 激活函数，旨在保持输入输出的方差一致。  

  - **Kaiming (He) 初始化**：专为 ReLU / LeakyReLU 设计，考虑到零点裁剪造成的方差减半，采用
    $$
    \sqrt{2 / \text{fan_in}}
    $$
     缩放因子。  

### 第三阶段：代码实验（约 45 分钟）

#### 目录结构演进

在 `GeoAI-Learning` 项目的 `pytorch` 目录下新建 `Day24` 文件夹：  

```
GeoAI-Learning/
│
├── python_basic/
│   ├── Day14/
│   └── Day15/
│
└── pytorch/
    ├── Day16/
    ├── Day17/
    ├── Day18/
    ├── Day19/
    ├── Day20/
    ├── Day21/
    ├── Day22/
    ├── Day23/
    └── Day24/
        └── activation_function.py
```

#### 实验任务

创建 `activation_function.py` 文件，编写并运行以下代码，实现激活函数（Sigmoid、Tanh、ReLU）对比、Softmax 概率计算以及参数初始化实战：  

```python
"""
程序名称：activation_function.py
作者：黄乐源
日期：2026-07-21
描述：PyTorch 常用激活函数特性对比 (Sigmoid, Tanh, ReLU, Softmax) 与参数初始化 (Xavier / Kaiming) 实战
"""

import torch
import torch.nn as nn
import matplotlib.pyplot as plt

print("================= 任务 1：Sigmoid, Tanh 与 ReLU 激活函数可视化对比 =================")

# 生成区间 [-6, 6] 的连续数据
x = torch.linspace(-6, 6, 300)

# 实例化激活函数模块
sigmoid_func = nn.Sigmoid()
tanh_func = nn.Tanh()
relu_func = nn.ReLU()

y_sigmoid = sigmoid_func(x)
y_tanh = tanh_func(x)
y_relu = relu_func(x)

# 绘制三图对比
plt.figure(figsize=(14, 4))

# 1. Sigmoid
plt.subplot(1, 3, 1)
plt.plot(x.numpy(), y_sigmoid.numpy(), color='blue', linewidth=2, label='Sigmoid')
plt.axhline(0, color='black', linestyle='--', alpha=0.4)
plt.axhline(1, color='black', linestyle='--', alpha=0.4)
plt.title("Sigmoid Activation")
plt.xlabel("Input x")
plt.ylabel("Output y")
plt.grid(True)
plt.legend()

# 2. Tanh
plt.subplot(1, 3, 2)
plt.plot(x.numpy(), y_tanh.numpy(), color='green', linewidth=2, label='Tanh')
plt.axhline(-1, color='black', linestyle='--', alpha=0.4)
plt.axhline(1, color='black', linestyle='--', alpha=0.4)
plt.title("Tanh Activation")
plt.xlabel("Input x")
plt.ylabel("Output y")
plt.grid(True)
plt.legend()

# 3. ReLU
plt.subplot(1, 3, 3)
plt.plot(x.numpy(), y_relu.numpy(), color='red', linewidth=2, label='ReLU')
plt.axhline(0, color='black', linestyle='--', alpha=0.4)
plt.title("ReLU Activation")
plt.xlabel("Input x")
plt.ylabel("Output y")
plt.grid(True)
plt.legend()

plt.tight_layout()
plt.show()

print("\n================= 任务 2：Softmax 多分类概率转换实验 =================")

# 假设神经网络对 3 个样本的 4 分类 Logits 输出
logits = torch.tensor([
    [2.0, 1.0, 0.1, 0.5],
    [0.5, 3.2, 0.1, 0.2],
    [-1.0, 0.0, 2.5, 1.1]
], dtype=torch.float32)

softmax_layer = nn.Softmax(dim=1)
probabilities = softmax_layer(logits)

print("原始 Logits得分:\n", logits)
print("\n经过 Softmax 转换后的类别概率:\n", probabilities)
print("\n每个样本所有类别概率之和:", torch.sum(probabilities, dim=1))

print("\n================= 任务 3：PyTorch 参数初始化实战 =================")

class CustomNet(nn.Module):
    def __init__(self):
        super(CustomNet, self).__init__()
        self.fc1 = nn.Linear(10, 20)
        self.relu = nn.ReLU()
        self.fc2 = nn.Linear(20, 5)

        # 执行参数初始化
        self._init_weights()

    def _init_weights(self):
        # 对 Linear 层做 Kaiming 正态分布初始化（针对 ReLU 激活）
        nn.init.kaiming_normal_(self.fc1.weight, nonlinearity='relu')
        nn.init.constant_(self.fc1.bias, 0.0)

        # 对最后一层输出层做 Xavier 均匀分布初始化
        nn.init.xavier_uniform_(self.fc2.weight)
        nn.init.constant_(self.fc2.bias, 0.0)

    def forward(self, x):
        x = self.relu(self.fc1(x))
        out = self.fc2(x)
        return out

net = CustomNet()
print("自定义网络权重 shape:", net.fc1.weight.shape)
print("fc1 权重前 2 行初始化数值示例:\n", net.fc1.weight[:2])
print("fc1 偏置前 5 项初始化数值示例:\n", net.fc1.bias[:5])

print("\n🏁 Day24 激活函数与参数初始化实验成功完成！")
```

### 第四阶段：GitHub 提交（约 15 分钟）

在终端验证代码无误后提交：

```bash
# 1. 检查变动
git status

# 2. 将变动加入暂存区
git add .

# 3. 提交规范 Commit 信息
git commit -m "Day24：完成激活函数与参数初始化学习"

# 4. 推送至 GitHub
git push
```

## 今日需要输出的成果

### 本地与 GitHub 仓库

- 建立 `pytorch/Day24/` 目录结构。  
- 完成 `pytorch/Day24/activation_function.py` 代码文件。  

### 笔记沉淀

- 整理 Sigmoid、Tanh、ReLU、Softmax 函数的公式、曲线特征、导数与主要优缺点对比表。  
- 总结参数全零初始化的危害、Xavier 与 Kaiming 初始化针对不同激活函数的匹配选择逻辑。  

### 截图建议（可选，建议保存在个人笔记中）

**控制台运行结果截图**：运行 `activation_function.py`，截取包含 Softmax 输出概率矩阵及其行和为 1 的验证结果，以及自定义网络权重/偏置初始化的控制台输出。  

![Day24_ActivationFunction运行结果](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day24\Day24_ActivationFunction运行结果.png)

**Matplotlib 可视化图表截图**：截取包含 Sigmoid、Tanh 和 ReLU 三种激活函数对比折线图的可视化窗口。  

![Day24_ActivationFunction可视化图表](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day24\Day24_ActivationFunction可视化图表.png)

**GitHub 阶段更新截图**：截取浏览器或 Git 客户端中，成功出现 `pytorch/Day24/` 目录以及 Commit 提交记录 `"Day24：完成激活函数与参数初始化学习"` 的界面。  

![Day24_Github仓库](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day24\Day24_Github仓库.png)

## Day24 完成标准

睡前满足以下 4 项即为 100% 完成：

1. 能清晰阐述 ReLU 函数的公式及其相比 Sigmoid/Tanh 的三大优势（计算简单、无单侧饱和梯度消失、稀疏激活）。  
2. 掌握 Softmax 函数将输入变换为概率分布的计算过程及其在多分类任务中的重要作用。  
3. 知道为何不能把权重全部初始化为 0，并能用 PyTorch 代码为网络层配置 Kaiming / Xavier 初始化。  
4. 成功在 GitHub 仓库中创建 `pytorch/Day24/` 并推送 `activation_function.py` 代码。  

## 今日完成

- 完成黑马 AI 大模型《神经网络与深度学习》（Day03-11 至 Day04-02）视频学习。  
- 深入掌握了 ReLU 激活函数的表达形式、梯度特性与优势。  
- 学习了 Softmax 激活函数在多分类任务输出层的概率归一化原理。  
- 理解了神经网络参数初始化的理论依据，掌握了对称性破坏（Symmetry Breaking）概念。  
- 独立编写并运行 `activation_function.py`，绘制了 Sigmoid、Tanh、ReLU 的函数曲线，验证了 Softmax 概率分布及 Kaiming / Xavier 初始化的代码实操。  
- 使用 `git commit -m "Day24：完成激活函数与参数初始化学习"` 完成 GitHub 代码提交与推送。  

## 今日收获

**Day03-11 ReLU 激活函数**：

- ReLU激活函数

  - 公式：
    $$
    f(x) = max(0, x)
    $$
    由公式可知：默认情况下，ReLU只考虑正样本。

  - 激活函数求导公式
    $$
    f'(x) = 0或1
    $$
    
- ReLU的函数图像如下：
  
  ![Day24_ReLU激活函数图像](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day24\Day24_ReLU激活函数图像（自绘）.png)
  
  由图像可知：**ReLU求导值永远是1**（因为原函数max(0, x)恒大于等于0），较之于Tanh激活函数求导值[0, 1]，在最优解的情况下求导值是1；而ReLU求导值永远是1，所以**收敛更新最快**，**效率比Tanh更高**。
  
- ReLU激活函数将小于0的值映射为0，而大于0的值则保持不变，它更加**重视正信号**，而**忽略负信号**，这种激活函数运算**更为简单**，能够**提高模型的训练效率**。
  
- 当x<0时，ReLU导数为0，而当x>0时，则不存在饱和问题。所以，ReLU能够**在x>0时保持梯度不衰减**，从而**缓解梯度消失**问题（梯度永远是1）。然而，随着训练的推进，部分输入会落入小于0区域，导致对应权重无法更新。这种现象被称为“神经元死亡”。
  
- ReLU是目前最常用的激活函数，与Sigmoid相比，其优势为：
  
  - 采用Sigmoid函数，计算量大（指数运算），反向传播求误差梯度时，计算量相对大，而采用ReLU激活函数，整个过程的计算量节省很多。
    - Sigmoid函数反向传播时，很容易就会出现梯度消失的情况，从而无法完成深层网络的训练。
    - ReLU会使一部分神经元的输出为0，这样就造成了**网络的稀疏性**（起到了筛选特征的作用），并且**减少了参数的相互依存关系**，**缓解了过拟合**问题的发生。
  
- 其他常见的激活函数图像如下：

  ![Day24_ReLU激活函数图像](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day24\Day24_其他常见的激活函数图像.png)

**Day03-12 Softmax 激活函数**：

- SoftMax激活函数

  - softmax用于**多分类**过程中，它是二分类函数sigmoid在多分类上的推广，目的是**将多分类的结果以概率的形式展现出来**。计算方法如下图所示：
    $$
    \text{softmax}(z_i) = \frac{e^{z_i}}{\sum_j e^{z_j}}
    $$
    
- Softmax就是将网络输出的logits通过softmax函数，就映射成为(0, 1)的值，而**这些值的累和为1**（满足概率的性质），那么我们将它理解成概率，**选取概率最大（也就是值对应最大的）节点**，作为我们的**预测目标类别**。（类似于遥感图像监督分类中的最大似然法）
  
  
  
- 激活函数的选择方法

  - 对于**隐藏层**：
    - 优先选择ReLU激活函数；
    - 如果ReLu效果不好，那么尝试其他激活，如Leaky ReLu等；
    - 如果你使用了ReLU， 需要注意一下Dead ReLU问题，避免出现0梯度从而导致过多的神经元死亡；
    - 少使用Sigmoid激活函数，可以尝试使用Tanh激活函数；
    - 总结：**ReLU > LeakyReLU > RReLU、PReLU > Tanh > Sigmoid**。

  - 对于**输出层**：
    - **二分类**问题选择**Sigmoid**激活函数；
    - **多分类**问题选择**Softmax**激活函数；
    - **回归**问题选择**Identity**激活函数（`f(x) = x, f'(x) = 1`）。

- 课后总结：

  - 1、激活函数的**作用**？
    - 答：向神经网络中**添加非线性元素**。
  - 2、常见的激活函数及其**特点**
    - Sigmoid：会把值映射到[0, 1]之间，求导范围[0, 0.25]，存在梯度消失的问题，适合浅层神经网络，多用于输出层；
    - Tanh：求导范围[0, 1]，收敛速度较Sigmoid更快；
    - ReLU：只考虑正样本，求导结果是0或1；
    - Softmax：将多分类的结果以概率的形式展现出来，选取概率最大的节点作为最终结果。
  - 3、激活函数的选择
    - 隐藏层：**ReLU > LeakyReLU > RReLU、PReLU > Tanh > Sigmoid**；
    - 输出层：**二分类用Sigmoid，多分类用Softmax，回归用Identity**。

**Day04-01 至 Day04-02 参数初始化**：

在构建网络之后，网络中的参数是需要初始化的。我们需要初始化的参数主要有**权重**和**偏置**，**偏置一般初始化为0即可**，而对权重的初始化则会更加重要。

- 参数初始化的**作用**：
  - **防止梯度消失或爆炸**：初始权重值过大或过小会导致梯度在反向传播中指数级增大或缩小。
  - **提高收敛速度**：合理的初始化使得网络的激活值分布适中，有助于梯度高效更新。
  - **保持对称性破除**：权重的初始化需要**打破对称性**，否则网络的**学习能力**会受到限制。

- 常见参数初始化**方法**：
  - **均匀分布初始化** `nn.init.uniform_()`：
    - 权重参数初始化**从区间均匀随机取值**，默认区间为(0, 1)。可以设置为在(-1 / √d, 1 / √d)均匀分布中生成当前神经元的权重，其中**d为神经元的输入数量**（当前层的上一层的神经元数量）。
    - 优点：能有效打破对称性。
    - 缺点：随机选择范围不当可能导致梯度问题。
    - 适用场景：浅层网络或低复杂度模型。隐藏层。
  - **正态分布初始化** `nn.init.normal_()`：
    - 随机初始化从**均值为0，标准差是1的高斯分布**中取样，使用一些**很小的值**对参数W进行初始化。
    - 优点：**能有效打破对称性**。
    - 缺点：随机选择范围不当**可能导致梯度问题**。
    - 适用场景：**浅层网络**或**低复杂度模型**。隐藏层。
  - **全0初始化** `nn.init.zeros_()`：
    - 将神经网络中的**所有权重参数初始化为0**。
    - 优点：**实现简单**。
    - 缺点：**无法打破对称性**，所有神经元更新方向相同，无法有效训练。
    - 适用场景：几乎不适用，仅用于**偏置项的初始化**。
  - **全1初始化** `nn.init.ones_()`：
    - 将神经网络中的**所有权重参数初始化为1**。
    - 优点：**实现简单**。
    - 缺点：
      - **无法打破对称性**，所有神经元更新方向相同，无法有效训练；
      - 会导致激活值在网络中呈指数增长，容易出现**梯度爆炸**。
    - 适用场景：
      - 测试或调试：比如验证神经网络是否能正常前向传播和反向传播；
      - 特殊模型结构：某些稀疏网络或特定的自定义网络中可能需要手动设置部分参数为1；
      - 偏置初始化：偶尔可以将偏置初始化为小的正值(如0.1)，但很少用1作为偏置的初始值。
  - **固定值初始化** `nn.init.constant_()`：
    - 将神经网络中的**所有权重参数初始化为某个固定值**。
    - 优点：**实现简单**。
    - 缺点：
      - **无法打破对称性**，所有神经元更新方向相同，无法有效训练；
      - 初始权重过大或过小可能导致**梯度爆炸**或**梯度消失**。
    - 适用场景：
      - 测试或调试：比如验证神经网络是否能正常前向传播和反向传播；
  - **kaiming初始化**（也叫做**HE初始化**）：
    - 专为ReLU和其变体设计，考虑到ReLU激活函数的特性，对输入维度进行缩放。
    - HE初始化分为正态分布的HE初始化和均匀分布的HE初始化。
      - **正态分布**的HE初始化 `nn.unit.kaiming_normal_()`：
        - 它是从[0, std]中抽取样本的，`std= sqrt(2 / fan_in)`，`fan_in`是输入层神经元的个数（特征个数）。
      - **均匀分布**的HE初始化 `nn.unit.kaiming_uniform_()`：
        - 它是从[-limit, limit]中的均匀分布中抽取样本的，limit是 `sqrt(6 / fan_in)`，`fan_in`是输入层神经元的个数（特征个数）。
    - HE初始化一般和ReLU激活函数搭配使用（**kaiming + ReLU**）。
    - 优点：**适合ReLU**，能保持梯度稳定。
    - 缺点：对**非ReLU**激活函数**效果一般**。
    - 适用场景：**深度网络**（**10层及以上**），使用**ReLU、Leaky ReLU**激活函数。
  - **xavier初始化**（也叫做**Glorot初始化**）：
    - 根据网络输入和输出的维度自动选择权重范围，使得输入和输出的方差相同。
    - 该方法也有两种，一种是正态分布的xavier初始化，一种是均匀分布的xavier初始化。
      - **正态分布**的Xavier初始化 `nn.unit.xavier_normal_()`：
        - 它是从[0, std]中抽取样本的，`std = sqrt(2 / (fan_in + fan_out))`，`fan_in`是输入层神经元的个数（特征个数），`fan_out`是输出层神经元个数。
      - **均匀分布**的Xavier初始化 `nn.unit.xavier_uniform_()`：
        - 它是从[-limit, limit]中的均匀分布中抽取样本的，limit是 `sqrt (6 / (fan_in + fan_out))`，`fan_in`是输入层神经元的个数（特征个数），`fan_out`是输出层神经元个数。
    - 优点：**适用于Sigmoid、Tanh**等激活函数，**解决梯度消失问题**。
    - 缺点：对**ReLU等激活函数表现欠佳**。
    - 适用场景：**深度网络**（**10层及以上**），使用**Sigmoid或Tanh**激活函数。
- 总结：
  - 1、常见的初始化方式：
    - 均匀分布初始化
    - 正态分布初始化
    - 全0初始化
    - 全1初始化
    - 固定值初始化
    - kaiming初始化
    - xavier初始化
  - 2、参数初始化方法的**选择**：
    - 根据**激活函数**选择：根据激活函数的类型选择对应的初始化方法
      - Sigmoid / Tanh：Xavier初始化；
      - ReLU / Leaky ReLU：kaiming初始化。
    - 根据**网络深度**选择：
      - 浅层网络：随机初始化即可；
      - 深层网络：需要考虑方差平衡，如Xavier初始化或kaiming初始化。

## 遇到的问题/注意事项。

注意：`sqrt()`函数用于**开根号**。

## 明日计划

- 攻克 Day25：掌握神经网络完整搭建流程，能够独立完成一个简单分类网络训练（`mlp_classifier.py`）！  
- 在 `pytorch/Day25/` 目录下完成 `mlp_classifier.py` 实验代码并提交 GitHub。  