# Day11（2026.07.08）

## 今日目标

告别静态的数据结构，正式迈入 Python 的“逻辑引擎”与“流程模块化”核心——**函数（Function）**。全面解锁函数的黑箱封装、参数动态传递、多重返回值、严谨的 Sphinx/Google 风格说明文档编写以及多层函数嵌套调用逻辑。通过纯 Python 语言独立编写“GeoAI 多源建筑物空间要素几何中心（Centroid）计算与拓扑重叠面积防御性审计系统”，为后续构建海量遥感解译 Pipeline（数据流水线）建立坚不可摧的工程级复用模块！

## 完成情况

- - [x] 观看视频
- - [x] 环境配置 / 代码实验
- - [x] 完成实验程序
- - [x] 提交代码至 GitHub

## 今日任务拆解（建议总耗时 3.0~4.0 小时）

### 第一阶段：观看视频（约 2.5 小时）

在 PyCharm 中跟随敲写函数底层逻辑，重点理解“形参实参映射”与“嵌套调用的执行栈”：

#### 第一部分：函数基础与结构化定义

- **57 函数介绍**（约 25 min）：理解为什么需要函数，认识函数作为“高内聚、低耦合”可复用代码块的核心价值。
- **58 函数定义**（约 25 min）：掌握标准的 `def` 语法，学习无参函数的声明与基础调用流程。

#### 第二部分：数据交互与工程规范

- **59 参数与返回值**（约 30 min）：深入研究形式参数（Parameter）与实际参数（Argument）的映射，攻克使用 `return` 关键字返回多重数据的机制。
- **60 函数说明文档**（约 25 min）：学习如何在函数体首行编写规范的 `Docstring`（文档字符串），建立工业级代码的可读性标准。
- **61 函数嵌套**（约 25 min）：理清函数 A 调用函数 B 时，Python 内存执行栈的入栈与出栈顺序，为后期的算法逻辑打下基础。

#### 第三部分：实战演练与案例分析

- **62 函数案例**（约 30 min）：将前面所学的参数传递、返回值机制与实际业务场景深度结合，通过实战案例彻底打通函数的基础应用链条。

### 第二阶段：理论学习（约 0.5 小时）

深入理解函数模块化在 GeoAI 遥感图像批处理与深度学习 Pipeline 中的核心桥梁作用：

1. **函数封装在遥感影像网格切片（Tiling）与特征提取流中的核心作用**：

   在做大型城市（如武汉市、宜昌市）的遥感影像建筑物提取时，单幅影像通常高达数个吉字节（GB），无法直接送入深度学习模型。我们需要将大图裁剪为 $256 \times 256$ 或 $512 \times 512$ 的小网格（Tiles）。通过将“单块网格提取”的逻辑封装进一个标准函数（例如 `extract_building_features(tile_matrix)`），便可以通过简单的循环或者多线程并行调用，高效处理数万张切片。函数是让代码从“面条式零散脚本”进化为“现代化解译系统”的唯一通道。

2. **说明文档（Docstring）与类型提示在多团队学术协作中的抗灾风险防御**：

   在 GeoAI 课题组进行跨学科协作时，你写的代码往往会被其他同门直接调用。没有说明文档的函数如同没有说明书的雷区。通过在函数内部严格编写规范的说明文档，清晰标注每一个参数的类型（如 `geo_dict: dict` 代表地理属性字典）、单位（如 `resolution: float` 代表分辨率，单位：米）以及返回值的物理含义，能够瞬间抹除沟通障碍，极大提升算法仓的模块健壮性。

### 第三阶段：代码实验（约 45 分钟）

#### 目录结构演进

在你的 `GeoAI-Learning` 项目的 `python_basic` 目录下，新建 `Day11` 文件夹。整体结构如下：

Plaintext

```
GeoAI-Learning
│
└── python_basic
    ├── Day10/
    └── Day11/
        └── calculator_function.py
```

#### 实验任务

创建 `calculator_function.py` 文件。手工编写并运行以下代码，前半部分掌握函数的声明、多返回值与文档查询，后半部分构建一个模拟“GeoAI 建筑物空间矢量要素中心点提取与多源标签重叠深度防御审计系统”：

Python

```
# -*- coding: utf-8 -*-
"""
程序名称：calculator_function.py
作者：黄乐源
日期：2026-07-08
描述：掌握函数的定义、多参数传递、多返回值及函数嵌套，模拟 GeoAI 要素空间审计与中心解析
"""

print("================= 任务 1：函数定义、参数交互与多返回值系统 =================")

def calculate_centroid_and_area(min_x, min_y, max_x, max_y):
    """
    计算标准建筑物外接矩形(Bounding Box)的几何中心坐标与估算面积
    
    参数:
    min_x (float): 左下角西经/东经经度坐标
    min_y (float): 左下角南纬/北纬纬度坐标
    max_x (float): 右上角西经/东经经度坐标
    max_y (float): 右上角南纬/北纬纬度坐标
    
    返回:
    tuple: 包含三个元素的元组 (center_x, center_y, estimated_area)
           - center_x (float): 中心点经度
           - center_y (float): 中心点纬度
           - estimated_area (float): 估算平面面积 (以度为单位的简化模拟计算)
    """
    # 逻辑计算：中心点为两端点的均值
    center_x = (min_x + max_x) / 2.0
    center_y = (min_y + max_y) / 2.0
    
    # 面积计算：宽 * 高
    width = max_x - min_x
    height = max_y - min_y
    estimated_area = width * height
    
    # 返回多重数据（Python 会自动自动组包为元组）
    return center_x, center_y, estimated_area

# 模拟华师城环学院课题组捕获的某栋新型建筑物矢量边界拓扑参数
b_min_x, b_min_y = 114.3700, 30.5300
b_max_x, b_max_y = 114.3750, 30.5340

print(f"📡 输入建筑物边界: Min({b_min_x}, {b_min_y}) -> Max({b_max_x}, {b_max_y})")

# 调用函数并执行多返回值解包
cx, cy, area = calculate_centroid_and_area(b_min_x, b_min_y, b_max_x, b_max_y)

print(f"🎯 解析成功 -> 中心经度: {cx:.4f} | 中心纬度: {cy:.4f} | 模拟图层面积: {area:.6f}")

# 利用 Python 内置 help() 查看我们手工编写的专业函数说明文档
print("\n📋 打印自定义函数的系统说明文档：")
print(calculate_centroid_and_area.__doc__)


print("\n================= 任务 2：GeoAI 多级函数嵌套调用与深度特征防御审计 =================")

def check_confidence_safety(confidence):
    """底层防御级子函数：校验遥感标签置信度是否达标"""
    if confidence >= 0.85:
        return True
    return False

def audit_geo_feature(block_id, feature_dict):
    """
    中层审计级核心函数：嵌套调用置信度检查函数，并对输入要素进行全方位解构说明
    """
    # 提取字典中的核心属性（使用 Day10 学过的安全机制 get()）
    f_class = feature_dict.get("class", "unknown")
    f_conf = feature_dict.get("confidence", 0.0)
    
    print(f"🔄 启动针对要素 [{block_id}] 的函数嵌套审计栈...")
    
    # 嵌套调用第一层底层子函数
    is_safe = check_confidence_safety(f_conf)
    
    # 根据子函数的布尔返回值，处理本层核心业务逻辑
    if is_safe and f_class == "building":
        status_report = f"🟩 要素安全通过：确认高置信度建筑物要素，准予送入 U-Net 模型进行分割。"
    elif is_safe:
        status_report = f"🟨 要素暂缓：虽置信度达标({f_conf})，但类别为 [{f_class}]，非主攻建筑物目标。"
    else:
        status_report = f"🟥 威胁拦截：要素置信度({f_conf})过低，判定为噪点脏数据，拒绝入库！"
        
    return status_report

# 模拟课题组待批量清洗处理的 GeoJSON Properties 斑块数据集
mock_tiles = {
    "GEO_PATCH_001": {"class": "building", "confidence": 0.94},
    "GEO_PATCH_002": {"class": "water", "confidence": 0.88},
    "GEO_PATCH_003": {"class": "building", "confidence": 0.62}
}

print("📊 启动多层嵌套函数处理流水线 Pipeline：")
for patch_name, properties in mock_tiles.items():
    # 执行审计主函数
    report = audit_geo_feature(patch_name, properties)
    print(f"📢 审计报告 -> {report}\n")

print("🏁 Day11 函数模块化封装与多级调用测试成功，数据流走向非常清晰！")
```

### 第四阶段：GitHub 提交（约 15 分钟）

代码在 PyCharm 中运行无误且终端完美输出后，启动你的 Git 工具链，进行**纯中文 Commit** 同步归档：

Bash

```
# 1. 检查 Day11 本地文件的修改与新增状态
git status

# 2. 将 Day11 文件夹下的代码和变动存入暂存区
git add python_basic/Day11/

# 3. 编写严谨的纯中文 Commit 说明文档
git commit -m "Day11: 掌握函数 def 声明、形参实参交互、多返回值解包与多层嵌套调用审计实战"

# 4. 同步推送至 GitHub 个人学习公开主分支
git push origin main
```

## 今日需要输出的成果

### 本地与 GitHub 仓库

- `python_basic/Day11/` 目录以及用于几何中心解算与嵌套审计的脚本代码 `calculator_function.py`。

### 笔记沉淀

- 详细阐述什么是函数的**形式参数（形参）\**和\**实际参数（实参）**，并举例说明二者在函数调用时的绑定关系。
- 用文字理清在 `audit_geo_feature` 函数调用 `check_confidence_safety` 时，Python 解释器在内存管理中“调用栈（Call Stack）”的压栈与弹栈变化。

### 截图建议（可选，建议保存在个人笔记中）

**控制台 help() 文档输出截图**：截取终端中完美打印出你手工为 `calculate_centroid_and_area` 编写的规范中文注释的模块画面。

![Day11_CalculatorFunction运行结果1](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day11_CalculatorFunction运行结果1.png)

**多层嵌套流水线运行结果截图**：运行 `calculator_function.py`，记录绿、黄、红三种不同遥感要素标签等级在函数嵌套过滤下的分类输出。

![Day11_CalculatorFunction运行结果2](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day11_CalculatorFunction运行结果2.png)

**GitHub 同步成功界面**：截取 GitHub 上出现 Day11 且 Commit 信息完美显示为 `"Day11: 掌握函数 def 声明、形参实参交互、多返回值解包与多层嵌套调用审计实战"` 的界面。

![Day11_Github仓库](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day11_Github仓库.png)

## Day11 完成标准

如果今晚睡觉前你满足以下 4 项，代表你的 Day11 任务 100% 完成：

1. 能够徒手写出标准的 Python 函数声明语法，并说明 `return` 在函数执行中的特殊终止效果。
2. 深刻理解为什么一个函数可以返回多个变量，并且知道在接收端如何用多个变量直接解包（Unpacking）。
3. 熟练掌握使用三对双引号 `"""` 为函数编写可供外部 `help()` 或 `.__doc__` 调用的工业级标准代码文档。
4. 成功将 Day11 目录下的函数代码同步至远程 GitHub，没有任何报错和冲突。

## 今日完成

- 深刻理解代码重构与函数封装的设计初衷，掌握使用 `def` 关键字创建和调用自定义函数的基础语法
- 掌握函数参数传递的底层本质，熟练实现多参数输入以及使用 `return` 返回多重数据并进行变量同步解包
- 树立现代工程级规范，学会编写规范的 `Docstring` 文档字符串，并能利用内置机制提取函数的自说明文档
- 攻克多级函数嵌套调用机制，理清函数嵌套时的参数流转与数据链返回逻辑，成功构建 GeoAI 级要素嵌套审计 Pipeline

## 今日收获

**57 函数介绍**：

函数是**组织好的**、**可复用的**、用来**实现特定功能**的**代码片段**，如：**print()**、**input()**是Python提前定义好的、可复用的、实现特定功能的内置函数。

**58 函数定义**：

函数定义的语法如下：

```python
# 定义函数
def 函数名(参数列表):
    函数体
    ......
    return 返回值
  
# 调用函数    
函数名(参数)
```

函数定义时的**参数列表与返回值语句是可有可无的**（由需求确定）。

**59 参数与返回值**：

**return**语句必须出现在**函数体的最后一行**。

函数定义时多个参数间用逗号（,）分隔。

**return**语句**只有返回功能**，而**没有输出打印的功能**。如果要输出，需要结合print()函数来实现。

**形参（形式参数**）：函数**定义时括号里**的参数，**只能在函数内部使用**（局部变量）。

**实参（实际参数）**：函数在**实际调用**时传入（括号里）的参数。

函数有多个返回值时，多个返回值会封装到**元组**中，也可以用**解包**操作获取多个返回值到不同变量中。

round()用于控制小数位数，如：`round(len, 1)`就是将变量len保留到1位小数。

**60 函数说明文档**：

**函数说明文档**是**写在函数开头**，**用三个引号包裹的字符串**，用于**解释函数**的功能、参数、返回值等信息，方便调用者清楚函数的作用及细节。

**help()函数**用于**查看函数说明文档**，如：`help(circle_area_len)`。此外，当鼠标悬浮在函数上时，函数说明文档会自动展示。

**61 函数嵌套**：

函数的**嵌套调用**指的是在一个函数中，又调用了另一个函数。

函数调用遵循**栈**结构，最后被调用的函数最先返回（即LIFO，Last In First Out，**后进先出**）。

**62 函数案例**：



## 遇到的问题/注意事项

注意：函数必须**先定义后调用执行**（函数定义时内部代码并未执行，在调用时才执行）。函数中通过**缩进**来**描述归属关系**。

## 明日计划

- 从函数的基础声明全面走向高级进阶，玩转 Python 动态语言中极具魅力的函数高级传参模式与高阶抽象。
- 攻克 **Day12**：解锁局部与全局变量作用域界限（`global`）、位置参数/关键字参数/可变不定长参数（`*args`, `**kwargs`）、以及深度学习中高频作为超参数传递的“函数对象”与简洁致命的 `lambda` 匿名表达式！