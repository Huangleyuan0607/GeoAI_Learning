# Day34（2026.07.31）

## 今日目标

- 掌握 CNN 图像分类任务的完整流程，理解图像数据在 Dataset 与 DataLoader 中的加载与批处理逻辑。  
- 理解卷积神经网络从特征提取到全连接层分类的端到端训练、测试与性能优化逻辑。  
- 在 `pytorch/Day34/` 目录下完成 `CNN_Image_Classification` 项目代码搭建（包含数据集加载、模型构建、训练、测试与调优流程），并提交代码至 GitHub。  

## 完成情况

- - [x] 观看视频
- - [x] 环境配置 / 代码实验
- - [x] 完成实验程序
- - [x] 提交代码至 GitHub

## 今日任务拆解（建议总耗时 4.0~4.5 小时）

### 第一阶段：观看视频（预估 2.0 小时）

在 PyCharm 中跟随学习，重点理解 CNN 在图像分类案例中的数据集处理、网络结构设计思路及完整代码实现：

- **黑马 AI 大模型《神经网络与深度学习》**：
  - **Day07-02 CNN图像分类案例_准备数据集**：掌握图像数据集的读取、图像预处理与 DataLoader 的批处理配置。
  - **Day07-03 CNN图像分类案例_搭建神经网络思路分析**：理解卷积层、激活函数、池化层与全连接层交替堆叠的设计思路。
  - **Day07-04 CNN图像分类案例_代码实现**：掌握模型前向传播、损失函数计算、反向传播与优化器更新的全流程编写。
  - **Day07-05 CNN图像分类案例_模型训练**：掌握训练循环（Training Loop）、Batch 迭代控制与 Epoch 损失/准确率监控。
  - **Day07-06 CNN图像分类案例_模型测试**：掌握在 `torch.no_grad()` 模式下评估测试集性能、计算准确率（Accuracy）及保存最优模型。
  - **Day07-07 CNN图像分类案例_优化及总结**：理解超参数调优（学习率、Batch Size）、Batch Normalization/Dropout 的作用及全流程复盘。

### 第二阶段：理论学习（约 1.0 小时）

系统梳理 CNN 图像分类任务的核心流程与架构规范：  

- **核心知识点**：

  - **图像分类任务流程**：
    $$
    数据加载与预处理 \rightarrow 特征提取(Conv + Pooling) \rightarrow 展平(Flatten) \rightarrow 维度分类(Linear) \rightarrow 损失计算(CrossEntropy) \rightarrow 优化更新(Optimizer)
    $$

  - **数据管道（Pipeline）**：使用 `torchvision.transforms` 完成图像尺寸缩放、张量转换与归一化。  

  - **评估与优化机制**：

    - **Evaluation**：测试阶段关闭梯度计算（`torch.no_grad()`）与切换为评估模式（`model.eval()`）。  
    - **Optimization**：通过数据增强、学习率调整及添加归一化/正则化层提升模型泛化能力。  

### 第三阶段：代码实验（约 1.0~1.5 小时）

#### 目录结构演进

在 `GeoAI-Learning` 项目的 `pytorch` 目录下新建 `Day34` 文件夹及项目代码：  

```
GeoAI-Learning/
│
└── pytorch/
    ├── Day33/
    └── Day34/
        └── CNN_Image_Classification/
            ├── dataset.py
            ├── model.py
            ├── train.py
            ├── test.py
            └── utils.py
            
```

#### 实验任务

创建 `CNN_Image_Classification` 项目，实现完整的 CNN 图像分类训练与测试流程。  

1、**`dataset.py`**（数据预处理与加载器构建）

```python
import torch
from torch.utils.data import DataLoader, TensorDataset
from torchvision import transforms

def get_data_loaders(batch_size=32):
    """
    模拟构建 Dataset 与 DataLoader 流水线
    包含简单的 Resize、ToTensor 与 Normalize 预处理逻辑
    """
    # 1. 定义数据预处理流水线 Pipeline
    transform = transforms.Compose([
        transforms.Normalize(mean=[0.5], std=[0.5])  # 图像张量归一化至 [-1, 1]
    ])

    # 2. 模拟 1000 张单通道 28x28 的图像数据及对应的 10 分类标签
    dummy_x = torch.randn(1000, 1, 28, 28)
    dummy_y = torch.randint(0, 10, (1000,))

    # 对模拟数据应用归一化变换
    dummy_x = transform(dummy_x)

    # 3. 划分训练集 (800张) 与测试集 (200张)
    train_dataset = TensorDataset(dummy_x[:800], dummy_y[:800])
    test_dataset = TensorDataset(dummy_x[800:], dummy_y[800:])

    # 4. 构建 DataLoader 批处理加载器
    train_loader = DataLoader(train_dataset, batch_size=batch_size, shuffle=True)
    test_loader = DataLoader(test_dataset, batch_size=batch_size, shuffle=False)

    return train_loader, test_loader

if __name__ == "__main__":
    train_loader, test_loader = get_data_loaders(batch_size=32)
    print(f"Train batches: {len(train_loader)}, Test batches: {len(test_loader)}")
```

2、**`model.py`**（CNN 分类模型定义）：

```python
import torch
import torch.nn as nn

class ConvClassifier(nn.Module):
    """用于图像分类的 simple CNN 网络"""
    def __init__(self, num_classes=10):
        super(ConvClassifier, self).__init__()
        self.features = nn.Sequential(
            nn.Conv2d(in_channels=1, out_channels=16, kernel_size=3, padding=1),
            nn.BatchNorm2d(16),
            nn.ReLU(),
            nn.MaxPool2d(kernel_size=2, stride=2),  # (B, 16, 14, 14)
            nn.Conv2d(in_channels=16, out_channels=32, kernel_size=3, padding=1),
            nn.BatchNorm2d(32),
            nn.ReLU(),
            nn.MaxPool2d(kernel_size=2, stride=2)   # (B, 32, 7, 7)
        )
        self.classifier = nn.Sequential(
            nn.Flatten(),
            nn.Linear(32 * 7 * 7, 128),
            nn.ReLU(),
            nn.Dropout(0.5),
            nn.Linear(128, num_classes)
        )

    def forward(self, x):
        x = self.features(x)
        x = self.classifier(x)
        return x
```

3、**`train.py`**（模拟训练与验证流程）：

```python

# 导包
import torch
import torch.nn as nn
import torch.optim as optim
import time
from model import ConvClassifer


def train_demo():
    """模拟构建(Batch_size = 32, Channel = 1, Height = 28, Width = 28)的图像数据与标签"""
    dummy_inputs = torch.rand(32, 1, 28, 28)
    dummy_labels = torch.randint(0, 10, (32,))

    model = ConvClassifer(num_classes = 10)
    criterion = nn.CrossEntropyLoss()
    optimizer = optim.Adam(model.parameters(), lr = 1e-3)

    model.train()
    for epoch in range(1, 11):
        # 定义变量，记录：总损失，总样本数据量，预测正确样本个数，训练（开始）时间
        total_loss, total_samples, total_correct, start = 0.0, 0, 0, time.time()
        # 模型预测
        outputs = model(dummy_inputs)
        # 计算损失
        loss = criterion(outputs, dummy_labels)

        # 梯度清零 + 反向传播 + 参数更新
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

        # 计算正确数
        total_correct += (torch.argmax(outputs, dim=-1) == dummy_labels).sum()
        total_loss += loss.item() * len(dummy_labels)
        total_samples += len(dummy_labels)

        print(f"Epoch: {epoch} / 10, Loss: {loss.item():.4f}, Acc: {total_correct / total_samples:.4f}, Time: {time.time() - start:.2f}s")

if __name__ == "__main__":
    train_demo()
```

4、**`test.py`**（模型测试与评估）：

```python
import torch
from model import ConvClassifier

def test_demo():
    dummy_inputs = torch.randn(10, 1, 28, 28)
    dummy_labels = torch.randint(0, 10, (10,))

    model = ConvClassifier(num_classes=10)
    model.eval()

    with torch.no_grad():
        outputs = model(dummy_inputs)
        _, predicted = torch.max(outputs, 1)
        correct = (predicted == dummy_labels).sum().item()
        accuracy = correct / dummy_labels.size(0)
        print(f"Test Accuracy: {accuracy * 100:.2f}%")

if __name__ == "__main__":
    test_demo()
```

5、**`utils.py`**（辅助工具函数：准确率计算与计算设备选择）

```python
import torch

def get_device():
    """获取当前可用的计算设备 (GPU / CPU)"""
    return torch.device("cuda" if torch.cuda.is_available() else "cpu")

def calculate_accuracy(outputs, targets):
    """
    计算当前 Batch 的分类预测准确率
    outputs: 模型输出的 Logits，形状为 (Batch_Size, Num_Classes)
    targets: 真实标签，形状为 (Batch_Size,)
    """
    _, preds = torch.max(outputs, dim=1)
    correct = (preds == targets).sum().item()
    return correct / targets.size(0)

if __name__ == "__main__":
    device = get_device()
    print(f"Using device: {device}")
```

### 第四阶段：GitHub 提交（约 15 分钟）

在终端验证代码运行无误后提交：  

```bash
# 1. 检查变动
git status

# 2. 将变动加入暂存区
git add .

# 3. 提交规范 Commit 信息
git commit -m "Day34：完成 CNN 图像分类全流程项目（数据、模型、训练、测试与调优）"

# 4. 推送至 GitHub
git push
```

## 今日需要输出的成果

### 本地与 GitHub 仓库

- 创建 pytorch/Day34/CNN_Image_Classification/ 目录。 
-  完成 dataset.py、model.py、train.py、test.py 与 utils.py 图像分类项目代码编写。  

### 笔记沉淀

- 完成 Day34 详细笔记，梳理 CNN 图像分类模型的完整 Pipeline 及模型训练/评估/优化阶段的核心细节。  

### 截图建议（可选，建议保存在个人笔记中）

**CNN 图像分类训练运行截图**：截取 PyCharm 控制台中运行 `train.py` 与 `test.py` 输出的 Loss 变化、测试准确率与模型训练维度校验结果。  

![Day34_Train运行结果](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day34\Day34_Train运行结果.png)

![Day34_Test运行结果](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day34\Day34_Test运行结果.png)

**GitHub 阶段更新截图**：截取浏览器或 Git 客户端中成功提交 `pytorch/Day34/` 目录及 Commit 记录 `"Day34：完成 CNN 图像分类全流程项目（数据、模型、训练、测试与调优）"` 的界面。  

![Day34_Github仓库](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day34\Day34_Github仓库.png)

## Day34 完成标准

睡前满足以下 4 项即为 100% 完成：

1. 熟练掌握图像数据在 PyTorch 中的加载、批处理与预处理机制。  
2. 理解 CNN 在图像分类任务中的整体数据流，能够推导任意卷积层与全连接层之间的特征图维度变化。  
3. 独立完成 `CNN_Image_Classification` 项目代码的模块化拆分（包含训练、测试与优化）与运行测试。  
4. 成功推送 `pytorch/Day34/CNN_Image_Classification/` 项目至 GitHub 仓库

## 今日完成

- 完成了黑马 Day07-02 至 Day07-07 视频的学习，系统掌握了 CNN 图像分类任务的全流程开发。
- 理解并实现了基于 `torchvision.transforms` 的图像预处理 Pipeline 与 DataLoader 的批处理配置。
- 在 `pytorch/Day34/` 目录下完成了 `CNN_Image_Classification` 项目代码的模块化拆分与编写（包含 `dataset.py`、`model.py`、`train.py`、`test.py` 及 `utils.py`）。
- 在模型中引入了 BatchNorm 与 Dropout 正则化结构，完成了模型训练循环、测试集无梯度评估（`torch.no_grad()`）及模型性能优化调参实验。
- 成功将 Day34 实验代码及笔记提交并推送至 GitHub 仓库。

## 今日收获

**Day07-02 至 Day07-04 CNN图像分类案例**：

- **CNN图像分类案例**

  - **准备数据集**

    - 数据集采用计算机视觉模块`torchvision`自带的**`CIFAR10`数据集**，包含6万张(32, 32, 3)图像，5万张训练集，1万张测试集，10个类别，每个类别6000张图像。

  - **搭建神经网络**

    - 我们要搭建的网络结构如下：

      ![Day34_网络结构](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day34\Day34_网络结构.png)

    - **卷积层参数计算公式**：

      - **卷积核参数 = 输入通道数 * 卷积核尺寸 * 卷积核数量 + 卷积核数量**，这里的**卷积核尺寸 = 卷积核长 * 卷积核宽**。

  - **模型训练**

    - 在训练时，使用**多分类交叉熵损失函数CrossEntropyLoss**和**Adam优化器**。

  - **模型测试**

    - 因为训练时用了`CrossEntropyLoss`，所以搭建网络时没有加`softmax()`激活函数，在模型预测时要用`argmax()`来模拟。

  - **优化及总结**

    - **优化思路**：
      - **增加卷积层数量**
      - **增加**每个卷积层的**卷积核数量**
      - **增加全连接层数量**
      - **增加全连接层参数量**
      - **修改激活函数**
      - **调整学习率**
      - **调整优化方法**
      - **增加训练轮数**

## 遇到的问题/注意事项

**注意**：

- 在实际的卷积神经网络中，**池化层步长**一般设置为**池化窗口的大小**，这样可以使得每次的池化窗口都不相交。
- 数据从最后一个池化层出来后，在进入第一个全连接层之前要经历一个**展平层**，将**4维数据(N, C, H, W) -> 二维数据(N, C * H * W)**。因为**全连接层只能处理二维数据**。

## 明日计划

- 进行 CNN 基础阶段的综合复习，整理卷积计算、特征图维度推导与池化机制的核心知识点。  
- 完成 Day35 Buffer Day 总结，优化并整理 `CNN_Project` 结构，完成 `CNN基础学习总结.md`。  
- 提交 Day35 总结笔记至 GitHub 仓库。  