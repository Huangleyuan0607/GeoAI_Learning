# Day12（2026.07.09）

## 今日目标

从函数的基础声明全面走向高级进阶，玩转 Python 动态语言中极具魅力的函数高级传参模式与高阶抽象。深入理解局部变量与全局变量的作用域隔离边界（`global`），攻克位置参数、关键字参数、默认参数与可变不定长参数（`*args`, `**kwargs`）的高级组合。掌握“函数作为参数”传递的闭包技巧，并精通极简高效的 `lambda` 匿名表达式，为后续 PyTorch 深度学习中自定义损失函数（Loss）、动态参数调整以及 PyCharm 调试大型 GeoAI 脚本扫清一切语法障碍！  

## 完成情况

- - [x] 观看视频
- - [x] 环境配置 / 代码实验
- - [x] 完成实验程序
- - [ ] 提交代码至 GitHub

## 今日任务拆解（建议总耗时 3.5~4.5 小时）

### 第一阶段：观看视频（约 3.0 小时）

在 PyCharm 中跟随敲写代码，理清参数在内存中的解构流程与匿名函数的即用即弃特性：  

#### 第一部分：变量作用域与传参机制进阶

- **63 变量作用域**（约 25 min）：理解局部变量（Local）与全局变量（Global）的生存周期，掌握 `global` 关键字在函数内部修改全局数据的安全界限。  
- **64 传参方式**（约 30 min）：理清位置参数、关键字参数的调用机制，深刻理解参数按位置或按名称对齐的逻辑。  

#### 第二部分：高级动态参数控制

- **65 默认参数**（约 25 min）：学习为函数定义默认缺省值（默认参数必须在普通参数之后），提高代码在日常调用中的灵活性。  
- **66 不定长参数**（约 35 min）：彻底攻克可变位置参数 `*args`（自动组包为元组）与可变关键字参数 `**kwargs`（自动组包为字典）的黑科技传参手法。  

#### 第三部分：高阶函数与匿名计算

- **67 函数作为参数**（约 30 min）：理解在 Python 中“一切皆对象”，掌握将一个函数作为实参传递给另一个函数进行高阶逻辑抽象。  
- **68 lambda表达式**（约 25 min）：学习无名函数 `lambda` 的精简定义语法，掌握单行逻辑的高效就地解析技巧。  

### 第二阶段：理论学习（约 0.5 小时）

深入理解进阶参数设计与匿名函数在 PyTorch 模型重构与遥感超参数调优中的工程级价值：

1. **不定长参数 `\*args` 与 `\**kwargs` 在深度学习模型自定义层与中转配置中的妙用**：

   在 PyTorch 构建复杂的神经网络（如 U-Net 的 Block 结构）时，我们经常需要继承 `nn.Module` 并自定义前向传播网络层。由于上游特征图（Feature Map）或者参数配置极其多变，我们无法预测后续会传入多少个超参数。使用 `def __init__(self, in_channels, *args, **kwargs):` 的定义方式，配合 `super().__init__(*args, **kwargs)`，可以用极简、弹性的方式将各种学习率、Dropout 率或通道数一揽子安全地中转给基类，极大地精简了网络构建的代码量。

2. **`lambda` 表达式与高阶函数在多源遥感动态波段线性重映射中的快速演练**：

   在进行遥感像元值（DN值）的归一化或者红外波段动态拉伸时，我们常需要将不同的变换公式（如最大最小值拉伸、高斯拉伸等）作为算法算子注入主执行引擎。通过定义一个接收数据和变换算法高阶函数，并在实际执行时直接塞入一个 `lambda x: (x - min_val) / (max_val - min_val)`，能够免去定义无数个一次性微型函数的麻烦，实现空间计算像元级的高速流式处理。

### 第三阶段：代码实验（约 45 分钟）

#### 目录结构演进

在你的 `GeoAI-Learning` 项目的 `python_basic` 目录下，新建 `Day12` 文件夹。整体结构如下：  

Plaintext

```
GeoAI-Learning
│
└── python_basic
    ├── Day11/
    └── Day12/
        └── factorial_recursive.py
```

#### 实验任务

创建 `factorial_recursive.py` 文件。手工编写并运行以下代码，前半部分深度演练位置/关键字/不定长参数的传递与作用域修改，后半部分构建一个模拟“多源遥感影像地物分类器超参数（Hyperparameters）自动化动态调优与基于 lambda 表达式的像元变换算子系统”：  

```python
# -*- coding: utf-8 -*-
"""
程序名称：factorial_recursive.py
作者：黄乐源
日期：2026-07-09
描述：掌握变量作用域、四种核心传参、函数作为参数传递及 lambda 表达式，模拟遥感超参数调优系统
"""

print("================= 任务 1：全局变量安全修改与不定长参数解构演练 =================")

# 声明一个全局计数器，用于记录华中师范大学城市与环境科学学院 GeoAI 模型调优的总迭代次数
total_runs = 0

def configure_training_pipeline(model_name, learning_rate=0.001, *args, **kwargs):
    """
    配置并模拟启动深度学习模型训练流水线
    
    参数:
    model_name (str): 模型名称
    learning_rate (float): 初始学习率，默认为 0.001
    *args: 不定长位置参数，用于接收额外附加选项
    **kwargs: 不定长关键字参数，用于接收动态超参数（如 batch_size, epochs）
    """
    # 声明并引入全局变量进行修改
    global total_runs
    total_runs += 1
    
    print(f"🎬 第 {total_runs} 次运行：配置 [{model_name}] 训练流水线...")
    print(f"   💡 基础参数 -> 初始学习率 (Learning Rate): {learning_rate}")
    
    # 检查是否有位置扩展参数 (args 会被自动包装成元组)
    if args:
        print(f"   📦 附加选项列表 (args): {args}")
        
    # 检查是否有关键字扩展参数 (kwargs 会被自动包装成字典)
    if kwargs:
        print(f"   ⚙️ 动态超参数集 (kwargs):")
        for hyper_key, hyper_val in kwargs.items():
            print(f"      - {hyper_key}: {hyper_val}")

# 1. 采用普通位置参数与默认参数调用
configure_training_pipeline("U-Net-Basic")

# 2. 采用关键字参数与位置+关键字不定长参数进行全能调用（模拟输入多维度训练配置）
configure_training_pipeline(
    "Geo-Transformer-V2",     # model_name (位置参数)
    0.0005,                   # learning_rate (覆盖默认值)
    "启用数据增强", "开启GPU加速", # args (不定长位置参数，会被包装为元组)
    batch_size=32,            # kwargs (以下均为不定长关键字参数，会被包装为字典)
    epochs=150,
    backbone="ResNet-50",
    loss_function="DiceLoss"
)


print("\n================= 任务 2：高阶函数调用与 lambda 表达式像元线性重映射 =================")

def apply_pixel_transformation(raw_value, transform_operator):
    """
    高阶函数：接收一个原始像元值，并调用传入的特定数学算子进行像素级重映射
    
    参数:
    raw_value (float): 原始遥感像元DN值/反射率
    transform_operator (function): 接收一个数值并返回变换后数值的算子函数 (高阶形参)
    """
    # 调用传入的函数对象执行核心数学计算
    transformed_value = transform_operator(raw_value)
    return transformed_value

# 场景：有一批野外建筑物提取中获取的多波段高分像元反射率，需要进行不同算子转换
raw_pixel = 128.0
print(f"🛰️ 原始探测区域像元值: {raw_pixel}")

# 方案 A：利用 lambda 极简构建一个归一化算子： 将像元线性压缩至 [0, 1] 空间 (假设最大值为 255)
normalize_op = lambda x: x / 255.0
norm_pixel = apply_pixel_transformation(raw_pixel, normalize_op)
print(f"📐 经过归一化算子变换 [lambda x: x / 255.0] 结果: {norm_pixel:.4f}")

# 方案 B：就地编写一个复杂的非线性拉伸算子，直接送入高阶函数运行
enhanced_pixel = apply_pixel_transformation(
    raw_pixel, 
    lambda x: (x * 1.2) if x < 150 else (x * 0.8)
)
print(f"⚡ 经过自适应增强算子 [lambda x: x * 1.2 if x < 150 else ...] 结果: {enhanced_pixel:.2f}")

print("\n🏁 Day12 函数高级进阶实验全部跑通，GeoAI 动态超参配置及高阶变换算子部署成功！")
```

### 第四阶段：GitHub 提交（约 15 分钟）

代码验证无误且运行顺畅后，打开终端，使用你熟悉的 Git 终端工具链进行规范的**纯中文 Commit** 提交：

```bash
# 1. 检查 Day12 本地文件夹的修改与新增状态
git status

# 2. 将 Day12 文件夹下的全部变动文件暂存
git add python_basic/Day12/

# 3. 严格遵循规范，编写详细的纯中文 Commit 备注
git commit -m "Day12: 掌握变量作用域隔离、不定长参数传递与 lambda 匿名高阶函数应用实战"

# 4. 同步推送至 GitHub 远程 main 分支
git push origin main
```

  

## 今日需要输出的成果

### 本地与 GitHub 仓库

- `python_basic/Day12/` 目录以及用于动态调优与拉伸转换的代码脚本 `factorial_recursive.py`。  

### 笔记沉淀

- 总结并解释：使用 `global` 关键字声明变量时，局部作用域和全局作用域在内存中指针引用的变化机制。
- 梳理参数定义的优先级顺序：阐述为什么默认参数、`*args` 以及 `**kwargs` 在定义函数时不能颠倒顺序。

### 截图建议（可选，建议保存在个人笔记中）

**不定长参数组包输出截图**：运行 `factorial_recursive.py`，观察控制台如何把 `args` 打印为元组、把 `kwargs` 打印为精美的字典。

**lambda 像元变换控制台截图**：观察像元经过归一化及非线性变换算子计算后的精确浮点数值输出。

![Day12_FactorialRecursive运行结果](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day12_FactorialRecursive运行结果.png)

**GitHub 同步成功界面**：截取 GitHub 上出现 Day12 且 Commit 信息完美显示为 `"Day12: 掌握变量作用域隔离、不定长参数传递与 lambda 匿名高阶函数应用实战"` 的界面。

![Day12_Github仓库](D:\CCNU\PreLearning_Plan\GeoAI-Learning\notes\Screenshot\Day12_Github仓库.png)

## Day12 完成标准

如果今晚睡觉前你满足以下 4 项，代表你的 Day12 任务 **100% 完成**：

1. 能够口头阐述局部变量和全局变量的区别，并能熟练在函数内部通过 `global` 声明修改外部全局数值。
2. 清楚知道 `*args` 和 `**kwargs` 分别代表什么含义，以及它们在函数定义与函数体内部调用时的形式差异。
3. 能够独立写出高阶函数，将一个独立的函数对象（或者 lambda 算子）作为参数传入另一个函数中安全执行。
4. 成功将 Day12 目录下的代码同步推送至远程 GitHub 仓库，中文提交备注绝对规范。

## 今日完成

- 深刻掌握局部变量与全局变量的作用域生存周期，熟练使用 `global` 关键字解决多模型计数器的跨域修改
- 攻克 Python 高级参数调用规范，彻底理清位置参数、关键字参数与默认参数的对齐与排布位置约束
- 精通可变不定长参数 `*args` 与 `**kwargs` 的自动组包机制，成功建立超参数灵活导入与弹性中转配置体系
- 掌握“函数作为参数”的高阶抽象概念，学会熟练编写并就地运行 `lambda` 匿名表达式完成复杂的逻辑重映射

## 今日收获

**63 变量作用域**：

变量的**作用域**指的是变量的**作用范围**（标识这个变量在哪里可以使用，在哪里不可以使用）。

**全局变量**：**在函数之外定义**的变量，在**整个文件中（包括函数内部）都可以使用**（通常定义在文件顶部）。

**局部变量**：**在函数内部定义**的变量，**只能在函数内部使用**，外部无法访问（函数执行完毕后，会自动销毁其局部变量）。

**global**关键字用于明确的告诉Python解释器，**在函数中要使用全局变量**，使得可以**在函数内部修改全局变量的值**。

**64 传参方式**：

传参方式指的是，在调用函数时，**传递实参的方式**。主要有两种：

1、**位置参数**：调用函数时根据**函数定义时的位置**来传递参数。要求：**调用函数时参数顺序与定义函数时参数顺序完全一致**。

2、**关键字参数**：调用函数时**以函数定义时形参名称作为关键字**，以**“键=值”**的形式来传递参数（不要求顺序）。要求：**如果位置参数与关键字参数混用，关键字参**
**数必须在位置参数之后**（关键字参数之间，没有顺序要求）。

位置参数与关键字参数**对比**：

位置参数：

优点：简洁；

缺点：可读性差、易出错、维护难；

应用场景：参数少（不多于3个），且顺序自然。

关键字参数：

优点：可读性强、易维护和扩展；

缺点：代码繁琐；

应用场景：参数较多，或易混淆的场景。

**黄金法则：半年后回头看你今天写的代码，能否一眼看出每个参数的含义，如果不能，就应该使用关键字参数。一切以代码结构清晰明了（可读性）、便于维护（维护性）为目标。**

**65 默认参数**：

**默认参数**也称为**缺省参数**，用于在定义函数时，**为参数提供默认值**，调用函数时，可以不传递有默认值的函数。

函数调用时，如果为默认参数传递了值，则会修改默认的参数值；如果没有传递该参数，则直接使用默认值。

**66 不定长参数**：

**不定长参数**也叫**可变参数**，用于函数定义及调用时**参数个数不确定**（0个或多个）的场景。主要有两种类型：

1、**位置传递**（*args）：传递的所有匹配的位置参数都会被args变量收集，这些参数会合并封装为一个**元组**，**args是元组类型**（注意并不会封装关键字参数）。args只是约定俗成的变量名，并不是关键字，这里可以使用任何合法的变量名(如 *data)。

2、**关键字传递**（**kwargs）：参数是以"键=值"形式传递的关键字参数，这些"键=值"参数都会被kwargs接受，并合并封装为一个字典类型。kwargs只是约定俗成的变量名，并不是关键字，这里可以使用任何合法的变量名。

*args适用于处理数量不确定的数据；**kwargs适用于处理数量不确定的选项（函数的配置参数，用来定制函数的行为）。

**67 函数作为参数**：

函数的**参数类型**：

**普通参数**：数字、布尔、字符串、列表、元组、集合、字典等；

**特殊参数**：**函数**。

**68 lambda表达式**：

**匿名函数**指的是**没有名称**的函数，需要**通过lambda表达式来声明函数**，可以简化**简单函数**的编写（单行表达式），其语法为：`lambda 参数列表 : 函数体`。

**函数逻辑比较简单**（单行表达式）且**只在一个地方使用**时，可以考虑使用匿名函数。简化书写（通常作为高阶函数的参数使用）。

匿名函数中**可以返回结果**，也**可以不返回结果**。返回结果时不需要写return，表达式的**运行结果就是要返回的结果**。

**sort()**实质也是函数，其有两个参数，默认为：`sort(key = None, reverse = False)`。这里的**key指的是排序依据**，reverse指的是是否反转顺序（默认为False，即**升序**）。

当**函数逻辑复杂**，需要**多步操作**，需要**多个地方重复使用**或需要**加文档说明**时，适合用**命名函数**。

## 遇到的问题/注意事项

注意：在基于global声明全局变量时，要**先声明再使用**。

注意：尽量**避免在函数中使用全局变量**，因为会使代码难以维护和调试。转而考虑**使用函数参数和返回值来传递数据**，不 依赖全局变量。global主要用在程序的状态、配置和计数器等场景中。

注意：**默认参数必须放在没有默认值的参数列表后面**，一个函数在定义时是**可以设置多个默认参数**的。

## 明日计划

- 结束 Python 中多功能函数块的封装，跨入复杂自组织逻辑——**递归函数与模块包开发**的深度阵地。  
- 攻克 **Day13**：解锁函数递归调用的终极奥秘，攻克类型注解（Type Hints）在深度学习中的防御用法，并掌握模块（Module）导入与自定义包（Package）的完美设计，打通可供全学院甚至学术界调用的 Python 库开发之路！  